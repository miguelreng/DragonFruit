# Implementation Plans

Generated by the `improve` skill on 2026-06-10, audited against commit `866fb1777f`.
Scope of the audit: the DragonFruit fork's custom **Atlas** surface only (agent
runtime, meeting notes / calendar, doc-review editor, custom web UI, Copilot mac
app) — **not** upstream Plane code. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                                                                        | Priority | Effort  | Depends on            | Status                                                                                                                                                                                                                                                                                                |
| ---- | ------------------------------------------------------------------------------------------------------------ | -------- | ------- | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 001  | Validate MCP server URLs to block SSRF                                                                       | P1       | S–M     | —                     | DONE (merged to main, commit `a107033485`)                                                                                                                                                                                                                                                            |
| 002  | One-command test baseline + Atlas characterization tests                                                     | P1       | S+M     | —                     | DONE (merged `bbcba7b35a`): root `pnpm test` + 6 Atlas characterization tests. (The 87 pre-existing failures it surfaced were triaged in 007/008/009/010 → suite now 0 failed.)                                                                                                                       |
| 003  | Consolidate duplicated server-side HTML builders                                                             | P3       | S       | (002 helps)           | DONE (merged `982d3e92c5`): plane/utils/html_builders.py + 27 unit tests                                                                                                                                                                                                                              |
| 004  | Cut re-render / decoration churn (chat, bookmarks, doc-review)                                               | P2       | M       | —                     | DONE (merged `23e191b039`): MessageRow memo, cached DecorationSet, list-view virtualization                                                                                                                                                                                                           |
| 005  | Split the three Atlas "god files"                                                                            | P3       | L       | 002 ✓                 | DONE — Python merged `ae5fe378a7` (chat.py 1905→1611, calendar/base.py 1606→1384); Swift merged `a5a6a1d765` (MeetingStore.swift 4292→3991, SystemAudioCapture.swift extracted, xcodebuild verified). Runtime audio-capture smoke via `pnpm mac:dev` before any DMG release.                          |
| 006  | (spike) Doc-write safety net: undo / multi-select / draft                                                    | P2       | M       | —                     | DONE (findings in `006-findings.md`): recommend multi-select first, then snapshot-revert; defer draft mode. Proposed plans 012/013.                                                                                                                                                                   |
| 007  | Triage + clear pre-existing API test-suite failures                                                          | P1       | M       | 002 ✓                 | DONE (merged `e4ff02d522`): 64→13 failures; residual 13 are real product-bug candidates, see 008                                                                                                                                                                                                      |
| 008  | Fix the real product bugs surfaced by 007                                                                    | P1       | M       | 007 ✓                 | DONE (merged `8a5ece2359`): 5 bugs incl. a security fix; suite 13→2. 2 residuals → 009                                                                                                                                                                                                                |
| 009  | Permission tests: expect 403 (not 404) for inaccessible projects                                             | P2       | S       | 010 ✓                 | DONE (merged `2dd23800f2`): kept intentional uniform-403; fixed 2 tests                                                                                                                                                                                                                               |
| 010  | Fix test-suite state isolation (full run non-deterministic)                                                  | P2       | M       | —                     | DONE (merged `bd128075ac`): suite now deterministic                                                                                                                                                                                                                                                   |
| 011  | Run the API pytest suite in CI (Postgres+Redis services)                                                     | P1       | S       | 002/007/008/009/010 ✓ | DONE (merged `f8dedc799e`): test-api job gates apps/api/\*\* PRs to preview. First real run = next qualifying PR.                                                                                                                                                                                     |
| 012  | Doc-write proposal multi-select (in-doc + Accept/Reject selected)                                            | P2       | S–M     | 006                   | DONE (merged `0714cde538`): static-verified; additive (Accept all/Reject all unchanged). Runtime UI smoke pending.                                                                                                                                                                                    |
| 013  | Doc-write "Discard Atlas changes" (session-snapshot revert)                                                  | P2       | S–M     | —                     | BUILT, HELD on `advisor/013-doc-write-discard-revert` — content-replacing (reverts manual edits too) + web auto-deploys; needs a runtime smoke before merge.                                                                                                                                          |
| 014  | Atlas personality (shared persona core, playful tone)                                                        | P2       | S       | —                     | DONE (merged `a6bf6bdfae`): plane/llm/persona.py ATLAS_PERSONA across chat + task/page-comment agents + web mirror. No personality SETTINGS (deliberate — one fixed identity).                                                                                                                        |
| 019  | Convert Markdown files and folders into a Wiki collection                                                    | P2       | M       | —                     | DONE (shipped `5d46c5b757`) — import preview modal + folder/bulk "Create wiki" + Wiki settings (publish/slug/accent/order) + published build-reader-style link. Live on prod. Follow-up UX → 025.                                                                                                     |
| 020  | Make Power K open instantly                                                                                  | P1       | S       | —                     | DONE — Power K now mounts/unmounts immediately with no modal/backdrop transition or delayed close reset.                                                                                                                                                                                              |
| 021  | Make popovers scale from their trigger                                                                       | P2       | M       | —                     | DONE — shared dropdown origins now read Base UI/Popper placement metadata, including nested menu wrappers.                                                                                                                                                                                            |
| 022  | Remove layout transitions from default primitives                                                            | P1       | S       | —                     | DONE — default UI/Propel buttons and UI cards no longer inherit `t-resize` width/height transitions.                                                                                                                                                                                                  |
| 023  | Preserve feedback in reduced motion                                                                          | P2       | M       | —                     | DONE — reduced-motion now drops movement while preserving color/opacity feedback; shared scale-from-zero primitives softened.                                                                                                                                                                         |
| 024  | Clean editor motion primitives                                                                               | P1       | M       | —                     | DONE — editor checkbox, fade, table insert, and width transitions use explicit non-layout motion.                                                                                                                                                                                                     |
| 025  | Wiki publish visibility (success step, link access, unpublish)                                               | P1       | S–M     | 019 (built)           | DONE (shipped 5d46c5b757 + follow-ups) — in-modal success view w/ View wiki primary, Published markers, Share popover on the folder header (status/link/view/settings/unpublish), owner preview of unpublished wikis. Live on prod.                                                                   |
| 026  | Atlas doc-write cosmetic formatting (markdown-lite proposals)                                                | P1       | M       | spacer fix f7eee663c1 | DONE — proposals now speak a constrained Markdown subset rendered server-side (headings, bold/italic/strike, code, links, flat lists, checklists, quotes, dividers) + [blank] spacers; prompts teach formatting-only replaces. Deferred: tables, colors, alignment, images, mark-preserving replaces. |
| 027  | Ship a private, per-user Momentum gamification system                                                        | P2       | L       | —                     | TODO — idempotent XP ledger, levels, timezone-aware streaks, achievements, backfill, current-user API, and Home Momentum card. No leaderboard/redeemable rewards/mobile UI in V1.                                                                                                                     |
| 028  | Create a Doc or Task from a Sticky                                                                           | P2       | S–M     | —                     | DONE — web Sticky toolbar action creates private Docs or default-state Tasks from a fresh click-time snapshot, with eligible-project filtering, recoverable errors, and Open toast actions. Unit tests, focused lint, and production build pass; runtime smoke is blocked by local API DNS.           |
| 029  | Show live collaborator avatars, pointers, and selections                                                     | P1       | M       | —                     | IN PROGRESS — base presence shipped in `a2560e6a07`; follow-up now guarantees the current user appears alone, uses real profile pictures with initials fallback, and pulses on active text editing. Editor/Web types and 4 focused state tests pass; signed-in two-browser smoke remains in 034.      |
| 030  | Make version history reliable in Docs, Briefs, and Sheets                                                    | P1       | M–L     | —                     | DONE (`a2560e6a07`) — page-type-aware capture, native previews, shared entry points, and non-destructive restore shipped; release regression closure is tracked in 034.                                                                                                                               |
| 031  | [Work from anywhere: OAuth MCP + complete safe actions](ai-native/031-work-from-anywhere.md)                 | P1       | M–L     | 032 for full promise  | TODO — validate with five token-based design partners first; then build a stable OAuth-authorized MCP endpoint, structured/annotated tools, approval parity, idempotency, audit, and external-client certification.                                                                                   |
| 032  | [Work that remembers: legible, correctable context](ai-native/032-work-that-remembers.md)                    | P1       | M       | —                     | TODO — build from the shipped Brief injection/read baseline; add Workspace Brief UI, memory correction/provenance, context proposals, run snapshots, inspector, and external-context parity.                                                                                                          |
| 033  | [Recurring operating loop: plan, approve, follow through, report](ai-native/033-recurring-operating-loop.md) | P1       | L       | 032; 031 for external | TODO — run a paid four-week concierge cohort before building; then implement a persisted weekly workflow state machine, proposal review, idempotent execution, blocker follow-up, and linked reporting.                                                                                               |
| 034  | Close shipped Beta-final items with regression evidence                                                      | P1       | S       | —                     | IN PROGRESS — screenshot follow-up now uses the configured accent for Atlas Doc mentions and replaces the browser PDF iframe with a custom in-app viewer. Ten focused Atlas/PDF/permission tests, Web types, and the Web production build pass; signed-in browser smoke remains.                      |
| 035  | Finish and release `dragonfruit.page` publishing                                                             | P1       | S–M     | —                     | IN PROGRESS — canonical URL tests, Web/Space types, Space build, and gateway syntax/public-only rewrite review pass. Production smoke and deployment/DNS remain gated on explicit authorization.                                                                                                      |
| 036  | Make the Atlas sidebar resizable and snappable                                                               | P1       | M       | —                     | IN PROGRESS — persisted 320–720 px width, 350 px snap/reset, accessible pointer/keyboard separator, and rail/full transitions implemented; 24 helper tests pass. Signed-in responsive browser smoke remains blocked by the local API/auth stack.                                                      |
| 037  | Redesign the Calendar event view modal                                                                       | P1       | M       | —                     | IN PROGRESS — View/Edit now share one responsive shell, icon-led details, chrome close, and primary meeting/calendar action; event transformations have 20 tests and payload semantics are preserved. Shared Create/Edit fields and signed-in visual smoke remain.                                    |
| 038  | Reproduce and fix the Atlas Mac App menu bug                                                                 | P1       | S–M     | repro criteria        | BLOCKED — backlog names no failing menu or symptom; run the matrix and capture a repeatable repro before touching SwiftUI lifecycle/menu code.                                                                                                                                                        |
| 039  | Replace routine empty-state illustrations with Solar icons                                                   | P2       | M       | —                     | IMPLEMENTED — routine Docs/tasks/bookmarks/stickies/workflows/activity/search/dashboard states use shared Solar icons; explanatory artwork remains. Rendering tests, Web types, and production build pass; production visual smoke remains.                                                           |
| 040  | Define the “Tooltip when selected” interaction                                                               | P1       | XS+S    | interaction contract  | BLOCKED — target object, trigger, content, placement, and dismissal are unspecified; discovery gate precedes implementation.                                                                                                                                                                          |
| 041  | Attach and embed Docs in task descriptions and comments                                                      | P1       | M–L     | —                     | IMPLEMENTED — Rich descriptions and Lite comments share one typed Page embed, workspace Doc picker, live card, and safe unavailable state. JSON/HTML round-trip, Editor/Web types, and production builds pass; production interaction smoke remains.                                                  |
| 042  | Remove Views and Intake from Project settings navigation                                                     | P2       | S       | —                     | IMPLEMENTED — Features settings retains Pages only; old Views and Intake settings URLs redirect to Project General while primary product routes/data remain. Navigation test, Web types, and build pass; production navigation smoke remains.                                                         |
| 043  | Finish custom-field dropdown layering above modals                                                           | P1       | S       | —                     | IN PROGRESS — both select portals use the documented modal layer and have a representative ModalCore story; UI/Web types and Storybook build pass. Runtime smoke of normal-page and stacked-modal consumers remains.                                                                                  |
| 044  | Favorite a Doc directly from its card                                                                        | P2       | S       | —                     | IMPLEMENTED — gallery, paper, and list cards expose the shared optimistic favorite action with accessible state and card interaction isolation. Metadata tests, Web types, and build pass; production synchronization smoke remains.                                                                  |
| 045  | Add a My Tasks table view with a Project column                                                              | P1       | L       | —                     | IMPLEMENTED — persisted per-workspace List/Table switch with sticky Task and visible Project, State, Priority, Assignee, Due date, Labels, and Updated columns sharing the existing task controller. Column tests, Web types, and build pass; production data smoke remains.                          |
| 046  | Evaluate Drawesome as a focused annotation mode                                                              | P3       | S spike | approved use case     | TODO — evaluate `drawesome@0.1.0` for a narrow Doc/PDF annotation use case; do not replace Excalidraw or add a production dependency before the decision gate.                                                                                                                                        |
| 047  | Finish filtering Files linked only to deleted Projects                                                       | P1       | S       | —                     | IN PROGRESS — deleted-Project filtering tests pass, and the File card menu now always exposes Delete: it is enabled for owners/admins and visibly explains the restriction otherwise. Permission tests, Web types, and build pass; query-count and signed-in Files smoke remain.                      |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Recommended order

1. **001** — security, low-risk, independent. Do first.
2. **002** — unblocks safe refactoring; independent of 001.
3. **003** and **004** — independent, can run in parallel after 002 (003 is trivial maintenance; 004 is the user-visible perf win).
4. **005** — only after 002 is green; highest-risk, pure-move refactor.
5. **006** — spike whenever; produces a decision, not a feature.

## AI-native strategy sequence (031–033)

The experiment and engineering orders are deliberately different:

1. **Run Plan 033 Phase 0 now** — validate the weekly operating loop manually and ask for payment before building orchestration.
2. **Run Plan 031 Phase 0 in parallel** — use the existing token MCP endpoint with five assisted design partners to learn which external-origin jobs repeat.
3. **Build Plan 032** — visible, correctable context is the shared foundation and the product's strongest durable advantage.
4. **Build Plan 031** — production OAuth, canonical tools, approval parity, and external-client distribution on top of the context layer.
5. **Build Plan 033** only after its payment/retention gate passes — reuse Plans 031/032 rather than creating another tool or context stack.

Do not implement permanent pricing/feature gates for these bets until the limited-Atlas activation and workspace-base-vs-seat packaging tests in the Source of Truth are reviewed.

## Beta-final execution sequence (034–047)

The screenshot was reconciled against `main` at `d2ca9cd196` and the current
dirty working tree on 2026-07-30. Execute in this order:

1. **034 — release closure**: verify the six already-shipped items and update
   the backlog; do not duplicate them.
2. **035 + 043 + 047 — finish work already in progress**: preserve the existing
   dirty public-domain, dropdown-layer, and deleted-Project File filtering
   changes, run their gates, and deploy 035 only after explicit authorization.
3. **036 + 037 — primary desktop UX**: Atlas sizing and the event modal are
   independent and can be implemented in either order.
4. **041 + 045 — larger editor/task work**: both are P1 but touch broad,
   stateful surfaces. Run separately and give each a runtime regression pass.
5. **039 + 042 + 044 — bounded polish/IA**: safe after release blockers; 039
   should be migrated in small feature batches.
6. **038 + 040 — requirement/reproduction gates**: no implementation until the
   missing symptom or interaction contract is captured.
7. **046 — optional spike**: evaluate only after a concrete annotation use case
   is approved; keep Excalidraw as the Whiteboard implementation.

Screenshot items mapped to plans:

| Screenshot item                                  | Disposition                                                                                  |
| ------------------------------------------------ | -------------------------------------------------------------------------------------------- |
| Resize and snap sidebar atlas                    | Plan 036                                                                                     |
| Collaborators online in docs editor              | Shipped `a2560e6a07`; Plan 034 verifies                                                      |
| `dragonfruit.page` added to publish settings     | Plan 035, already in progress                                                                |
| Redesign event view modal                        | Plan 037                                                                                     |
| Bug: Atlas Mac App menu                          | Plan 038, blocked on reproduction                                                            |
| Empty states with icons instead of illustrations | Plan 039                                                                                     |
| Highlights when mentioning docs in Atlas sidebar | Corrected in this session to use the configured accent color; Plan 034 runtime smoke remains |
| Files with deleted projects are not removed      | Filtering plus visible Delete action implemented; Plan 047 runtime smoke remains             |
| Tooltip when selected                            | Plan 040, blocked on interaction definition                                                  |
| Attach/embed Docs in task descriptions/comments  | Plan 041                                                                                     |
| Native PDF viewer                                | Custom in-app PDF viewer implemented in this session; Plan 034 runtime smoke remains         |
| Atlas works without task/workflow assignment     | Shipped `7633091f48`; Plan 034 verifies                                                      |
| Remove Intake and Views from Project settings    | Plan 042                                                                                     |
| Create-field dropdown below backdrop             | Plan 043, already in progress                                                                |
| Favorite a Doc from card                         | Plan 044                                                                                     |
| My Tasks view switch + Project column in table   | Plan 045                                                                                     |
| Drawesome                                        | Plan 046, gated spike                                                                        |
| Cross-project Doc tagging                        | Shipped `64e1f00e69`; Plan 034 verifies                                                      |

## Dependency notes

- **005 requires 002**: the god-file split is a behavior-preserving refactor and
  needs the `pytest -k atlas_baseline` net to prove it changed nothing.
- **003 benefits from 002** but doesn't strictly require it (it has its own unit
  tests). If both run, coordinate the HTML-helper moves to avoid conflicts.
- 001, 004, 006 are independent.
- **027 is independent** of the Atlas plans. It preserves the existing
  `/activity-summary/` endpoint and replaces only the unused Home activity card
  with the new private Momentum surface.
- **029 is independent** of the earlier plans. It reuses the existing
  Hocuspocus/Redis stack for ephemeral presence. Its Sheet work intentionally
  does not migrate `description_json.sheet_snapshot` to Yjs.
- **030 is independent of 029**. Version history stores bounded content
  snapshots and must never persist presence or mouse data. Either can ship first.
- **032 can start independently**, but its Brief history entry points should
  reuse 030 when that plan ships rather than create a second history model.
- **031 can validate before 032**, using existing token MCP access. Its complete
  product promise depends on 032's effective context package.
- **033 depends on 032** for durable context and context snapshots. External
  initiation/status depends on 031, but the native weekly loop can beta without it.
- **034 precedes new Beta-final work** so shipped items are removed from the
  active backlog and any true regression gets its own reproducible bug.
- **035, 043, and 047 are already dirty**. Executors must reconcile and
  preserve the current working-tree changes rather than recreate them from
  `HEAD`.
- **038 and 040 are deliberately blocked**. Their plans define the evidence
  needed to unblock implementation.
- **041 and 045 are independent**, but each changes a stateful task/editor
  surface; avoid overlapping execution in the single working tree.
- **046 has no production dependency relationship** until its Adopt gate
  passes. It must not replace the current Excalidraw Whiteboard.

## Findings considered and rejected (do not re-audit)

- **"Whisper temp-file leak on transcription failure"** — false positive. The
  `defer` cleanup in `MeetingStore.swift` `stopMeetingRecordingAndSave()` is
  installed at the top of the function (before the Whisper loop) and runs on every
  exit path; Whisper calls also have inner try/catch.
- **Most "N+1" perf claims in calendar/base.py** — the queries already use
  `.select_related("project", "state")`; the calendar-tasks query is bounded by the
  client's date range, not unbounded. Not worth a plan (a minor note: the date range
  is client-controlled, so an extreme range is possible — low value).
- **"Divergent HTML escaping = security risk"** — false premise. Both `chat.py`
  (`html.escape`) and `calendar/base.py` (`django.utils.html.escape`) escape
  correctly. The duplication is real but maintainability-only — captured as plan 003
  at P3, reframed honestly.
- **"OAuth token in logs"** — the current code does not log tokens; defensive-only,
  not evidenced. Not planned.
- **Title-generation race / memory-update transaction / missing deinit on Tasks** —
  real but low-impact (title is renameable; memory writes are upserts; Task callbacks
  use `[weak self]`). Recorded; not worth dedicated plans now.
- **MCP auth-header encryption, agent-chat permission decorators, live-server Zod
  validation, chrome-extension token storage, document HTML sanitization** — audited
  and confirmed correct (no issue).

## Discovered during execution (2026-06-10)

- **The API test suite is red on `main`.** Initial run showed 87 failures, but
  ~23 of those were just Redis being down; with **Postgres + Redis both up** the
  real number is **64 failures / 241 pass**. Validates finding #2 (pytest isn't in
  CI, so rot accumulated). Root causes, now diagnosed → **plan 007**:
  - DOMINANT (config): `base_host()` (`plane/authentication/views/common.py:59`)
    `.rstrip("/")` on a `None` URL because `WEB_URL`/`APP_BASE_URL` aren't set in
    `plane/settings/test.py` → `/auth/sign-in/` 500s, cascading into all 17
    `test_authentication.py` failures + ~10 direct + downstream cycle/project tests.
  - REAL BUG (6): `Page.owned_by cannot be both deferred and traversed using
select_related` — a bad `.defer()`/`.select_related()` query.
  - STALE TESTS: soft-delete manager test, Spanish "los" subject test, 3 `test_url.py`
    length tests.
  - ENV: broker-dependent bg-task tests (`kombu`/`.delay`) need mocking.
    **Plan 005 (god-file split) assumes a green baseline that does not exist — do 007
    first.**
- Local test DB: native Homebrew `postgresql@15` (no Docker), role/db `plane`/`plane`.
  Run the suite with `POSTGRES_HOST=localhost POSTGRES_PORT=5432 POSTGRES_USER=plane
POSTGRES_PASSWORD=plane POSTGRES_DB=plane REDIS_URL=redis://localhost:6379` prefixed
  to `pnpm test`.

## Feature plans from the 006 spike

- **012 — doc-write multi-select** (in-document selection + Accept/Reject selected):
  DONE & MERGED (`0714cde538`). Static-verified; Accept all/Reject all preserved.
  Runtime UI not smoke-tested in CI — eyeball the toggle + accept/reject-selected in
  a live Atlas doc-write session (it's additive, so the existing flow is unaffected).
- **013 — snapshot-revert** ("Discard Atlas changes"): BUILT & static-verified on
  branch `advisor/013-doc-write-discard-revert` (commits editor + `654f527e7f`).
  Session-start HTML snapshot (held in a listener ref) + "Discard Atlas changes"
  button (behind a `window.confirm`) that reverts via the in-place
  `replaceProviderDocumentFromHTML` then `clearAtlasReview` (new editor-ref method).
  Editor+web typecheck, editor build, lint green; accept/reject paths unchanged.
  **HELD — do NOT merge without a runtime smoke**: discard REPLACES document content
  (reverts manual edits too) and `web` auto-deploys to Vercel from `main`, so an
  unverified bug could lose user work. Smoke checklist in the plan's Test plan.
  Merge after smoke: `git merge advisor/013-doc-write-discard-revert`.

## 014 — Atlas personality (shipped)

DONE & MERGED (`a6bf6bdfae`). New `apps/api/plane/llm/persona.py` defines a single
`ATLAS_PERSONA` (sharp, funny, proactive, honest, kind, curious; light dry humor with
guardrails) — the chat persona, task-comment, and page-comment agents all import it,
and the web `ATLAS_IDENTITY.description` mirrors it. All tool/routing/execution-protocol
instructions preserved verbatim; suite 332 passed; web typecheck green. Tone dial lives
in `persona.py` (soften the "Genuinely funny" bullet to dial humor down).

## Agent autonomy: pause/resume + Mac-app & chat follow-ups (017 + 018) — shipped

- **017 — MERGED** (`cf14cbfafb`): agent runs can pause and resume. New `request_help`
  tool (Atlas asks a question → `needs_input` status, posts a comment), approve-to-execute
  for `ask`-policy tools, `POST /agent-runs/{id}/respond/` + `resume_agent_run` (re-grounds
  on recorded steps + the human's answer), and Notifications emitted on needs_input/completed.
  Non-resume path provably unchanged (349 baseline intact); 9 resume tests. **Resume is
  re-grounding, not serialized continuation.**
- **018 — MERGED** (`3890171ba7`): delivery. `GET /agent-runs/inbox/` (your actionable runs);
  Atlas **Mac app** polls it, fires a local notification on new needs_input, and a popover to
  answer/approve inline; Atlas **chat drawer** gets a "needs you" strip. Polling (live server
  not in prod). Backend 360 passed; Mac BUILD SUCCEEDED; web typecheck green.
- **Live only after deploy**: API → Railway (017+018 backend), web → Vercel (chat strip),
  Mac → new DMG. End-to-end smoke (assign task → Atlas `request_help` → reply in Mac/chat →
  it resumes) needs those deploys + the live LLM.
- Future: apply the same lifecycle to the page-comment agent; WebSocket push instead of polling.

## Wikipedia integration (015 + 016) — shipped

- **015 (A–E) — MERGED** (`ce51022165` A–C, `7aedf5232b` D–E): `lookup_wikipedia` agent
  tool (Atlas grounds facts + cites real URLs), `/wiki` slash command (cited summary
  block), doc-write grounding on Wikipedia reference material, and `/cite` (select a
  claim → inserts a Wikipedia citation link). Phase D shipped as an interim
  `/wiki-link` slash command (mention system was a closed type) — superseded by 016.
- **016 — MERGED** (`c8ed971e90`): the **real `@wiki` mention** — `@`-dropdown shows a
  "Wikipedia" section (3+ char queries, client-side; never hits workspace search),
  selecting inserts a linked mention chip, hovering shows a popover card
  (title/extract/thumbnail). Added `"wiki"` to `TSearchEntities`; removed the interim
  `/wiki-link`. Reuses `@plane/propel/popover` like `EditorUserMention`.
- All additive + typecheck-green; **runtime smoke pending** (web → Vercel): try `/wiki`,
  `/cite`, `@phot…` → Wikipedia section → hover the chip, and "ask Atlas a fact → cites Wikipedia".
- Future (from the 10-ideas list): auto-glossary, fact-check lens, multilingual, hero-image, LLM article-ranking.

## Convert docs/folder into internal Wiki (019) — planned

- **019 — TODO**: `plans/019-convert-docs-folder-to-wiki.md` defines a narrow V1 for
  importing many Markdown files or a local folder into a DragonFruit Docs folder
  collection. It builds on the current one-file Markdown importer and folder-backed
  Docs surface, adds a preview/confirm modal, creates one top-level folder plus one
  doc per Markdown file, and deliberately defers Atlas organization, non-Markdown
  formats, and nested folder navigation.

## Animation audit plans (020–024) — implemented

Generated by `/improve-animations` on 2026-07-11 against commit `841bb4897b`.
Implemented in the current working tree. Original recommended execution order:

1. **020** — remove Power K animation first; it is the clearest high-frequency
   violation and has a narrow write scope.
2. **022** — strip layout transitions from shared primitives next; it improves
   many buttons/cards without touching product logic.
3. **024** — clean editor-specific motion while the CSS audit context is fresh.
4. **021** — fix transform origins for positioned popovers/menus; verify in
   Storybook or the running app at 10% animation speed.
5. **023** — repair reduced-motion/shared primitive behavior; this is global, so
   do it after the narrower changes and smoke a few representative surfaces.

Dependencies: none are code-dependent, but review 021 and 023 together if both
touch `.t-dropdown` in `packages/tailwind-config/index.css`.

## Real product bugs surfaced by 007 (candidates for plan 008)

These are the residual failures after 007 — genuine product bugs (or stale tests),
NOT papered over. Evidence is the failing test that exposes each.

1. **Cycle create ignores URL `project_id`** (`test_cycles.py`, 3 fails) — `CycleCreateSerializer.validate()`
   reads `project_id` from `initial_data`, but the view (`plane/api/views/cycle.py:308`)
   passes only `context={"request": request}` and never injects `project_id` → 400
   "Project ID is required". Real bug.
2. **API-token PATCH can modify service tokens** (`test_api_token.py`) — the `patch()`
   queryset doesn't filter `is_service=False` (the `delete()` path does). Owner can
   patch a service token. Real bug.
3. **AI-connectors `secret` field crash** (`test_ai_connectors_app.py`, 2) —
   `WorkspaceAIConnectorSerializer` has a writable `secret` with no model field
   (model uses `secret_encrypted`); `create(**validated_data)` → `TypeError`. Real bug.
4. **Google-Drive attachment 409 id type mismatch** (`test_google_drive_attachment_app.py`) —
   409 response returns `id` as str, 201 returns a UUID; equality assert fails. Serializer
   field-type inconsistency. Likely real.
5. **copy_s3 task NOT NULL on `description_json`** (`test_copy_s3_objects.py`) — bg task
   `issue.save()` hits a NOT NULL constraint; also exercises real S3. Real bug + needs mock.
6. **Stale tests needing a ProjectMember / 403-vs-404 order** (`test_issue_app.py` x2,
   `test_page_app.py` x2, `test_project_bookmark_app.py`) — tests create a `Project`
   without a `ProjectMember`, so `ProjectEntityPermission` returns 403 before the view's
   404 lookup. Either test setup gaps (add ProjectMember) or an intended permission-order
   decision. Lower-stakes; verify intent before changing.

Recommend plan 008: fix items 1–5 (real bugs, each with the failing test as its
regression gate), and resolve item 6's tests. After 008, plan 005's "full suite green"
gate becomes achievable.

**008 DONE (merged `8a5ece2359`)** — all 5 real bugs fixed (each gated by its test),
plus 3 item-6 stale-test fixes. Suite 13→2. Two follow-ups surfaced:

- **009 — `allow_permission` 403-before-404 (cross-cutting).** For a non-existent or
  cross-workspace `project_id` the user has no membership for, `allow_permission`
  (`plane/app/permissions/base.py`) returns 403 before the view's `get_object_or_404`
  can return 404. Two tests assert 404 (`test_issue_app::...404_for_project_outside_workspace`,
  `test_project_bookmark::...handles_missing_project`). The executor correctly STOPPED
  rather than changing this — it affects every `@allow_permission`-decorated view, so
  it needs a deliberate, well-tested change.
- **010 — test-suite state isolation.** The FULL suite is non-deterministic: identical
  runs gave 2 vs ~12 failures because `transaction=True` tests (and `--reuse-db
--nomigrations`) leak committed rows across tests/runs — `test_projects.py` and
  `test_labels.py` fail in the full run but PASS in isolation. This must be fixed before
  "full suite green" is a reliable gate (for plan 005) or before pytest goes into CI
  (it would flake). Likely fix: ensure transactional tests truncate/clean up, or
  isolate them.

## Notes

- Production runs at `api.dragonfruit.sh` (Railway) + Vercel web, currently with **no
  live/HocusPocus server** — relevant to 004/006 (the editor often runs the REST
  fallback path in prod).
- The `improve` skill is read-only on source; these plans were written without
  modifying any source file. Use `improve execute <plan>` to dispatch an executor,
  or hand a plan to any agent.
