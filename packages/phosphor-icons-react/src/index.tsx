import * as React from "react";

export { Download as DownloadSimple, List as ListBullets, SquaresFour, X } from "@dragonfruit/icons";

export const IconContext = React.createContext({
  weight: "regular",
  size: "1em",
});
