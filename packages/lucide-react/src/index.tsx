import * as React from "react";
import * as Solar from "@solar-icons/react/ssr";

export * from "@plane/icons";

type LucideShimProps = React.ComponentPropsWithoutRef<"svg"> & {
  size?: number | string;
  color?: string;
  strokeWidth?: number | string;
};

const alias = (Icon: React.ComponentType<any>) =>
  function LucideAlias({ size, color = "currentColor", strokeWidth: _strokeWidth, width, height, ...rest }: LucideShimProps) {
    return <Icon {...rest} size={size ?? width ?? height ?? "1em"} color={color} />;
  };

const monitor = alias(Solar.Monitor);
const list = alias(Solar.List);
const pin = alias(Solar.Pin);
const camera = alias(Solar.Camera);
const crown = alias(Solar.Crown);
const chart = alias(Solar.Chart);
const batteryFull = alias(Solar.BatteryFull);
const batteryCharge = alias(Solar.BatteryCharge);
const book = alias(Solar.Book);
const cameraIcon = camera;
const questionCircle = alias(Solar.QuestionCircle);
const moon = alias(Solar.Moon);
const cloud = alias(Solar.Cloud);
const cloudRain = alias(Solar.CloudRain);
const code = alias(Solar.Code);
const cup = alias(Solar.Cup);
const command = alias(Solar.Command);
const compass = alias(Solar.Compass);
const cpu = alias(Solar.Cpu);
const crop = alias(Solar.Crop);
const database = alias(Solar.Database);
const filter = alias(Solar.Filter);
const flag = alias(Solar.Flag);
const gift = alias(Solar.Gift);
const heart = alias(Solar.Heart);
const key = alias(Solar.Key);
const map = alias(Solar.Map);
const minimize = alias(Solar.Minimize);
const sun = alias(Solar.Sun);
const telescope = alias(Solar.Telescope);
const branchDown = alias(Solar.BranchingPathsDown);
const branchUp = alias(Solar.BranchingPathsUp);
const fileRemove = alias(Solar.FileRemove);
const removeFolder = alias(Solar.RemoveFolder);
const figma = alias(Solar.Figma);
const sadCircle = alias(Solar.SadCircle);
const record = alias(Solar.Record);
const minusSquare = alias(Solar.MinusSquare);
const minusCircle = alias(Solar.MinusCircle);
const dollar = alias(Solar.Dollar);
const download = alias(Solar.Download);
const dropper = alias(Solar.Dropper);
const pen = alias(Solar.Pen);
const archive = alias(Solar.Archive);
const musicNote = alias(Solar.MusicNote);
const fileText = alias(Solar.FileText);
const gallery = alias(Solar.Gallery);
const videoFrame = alias(Solar.VideoFrame);
const threeSquares = alias(Solar.ThreeSquares);
const headphones = alias(Solar.HeadphonesRound);
const windowFrame = alias(Solar.WindowFrame);
const login = alias(Solar.Login);
const logout = alias(Solar.Logout);
const mapPoint = alias(Solar.MapPoint);
const microphone = alias(Solar.Microphone);
const chatRound = alias(Solar.ChatRound);
const arrowLeft = alias(Solar.ArrowLeft);
const arrowRight = alias(Solar.ArrowRight);
const arrowDown = alias(Solar.ArrowDown);
const arrowLeftDown = alias(Solar.ArrowLeftDown);
const arrowLeftUp = alias(Solar.ArrowLeftUp);
const arrowRightDown = alias(Solar.ArrowRightDown);
const arrowRightUp = alias(Solar.ArrowRightUp);
const arrowToTopLeft = alias(Solar.ArrowToTopLeft);
const arrowToTopRight = alias(Solar.ArrowToTopRight);
const settings = alias(Solar.Settings);
const settingsMinimalistic = alias(Solar.SettingsMinimalistic);
const link = alias(Solar.Link);
const addSquare = alias(Solar.AddSquare);

export const Airplay = monitor;
export const AlignJustify = list;
export const Anchor = pin;
export const Aperture = camera;
export const Book = book;
export const Award = crown;
export const BadgeCheck = alias(Solar.CheckCircle);
export const BarChart = chart;
export const Battery = batteryFull;
export const BatteryCharging = batteryCharge;
export const BookOpen = book;
export const BrainCog = settings;
export const Camera = cameraIcon;
export const CameraOff = camera;
export const Cast = monitor;
export const Chrome = windowFrame;
export const CircleChevronDown = arrowDown;
export const Moon = moon;
export const Cloud = cloud;
export const CloudDrizzle = cloud;
export const CloudLightning = cloud;
export const CloudRain = cloudRain;
export const CloudSnow = cloud;
export const Codepen = code;
export const Codesandbox = code;
export const Command = command;
export const Compass = compass;
export const Coffee = cup;
export const Cog = settings;
export const Columns = list;
export const CornerDownLeft = arrowLeftDown;
export const CornerLeftDown = arrowLeftDown;
export const CornerLeftUp = arrowLeftUp;
export const CornerRightDown = arrowRightDown;
export const CornerRightUp = arrowRightUp;
export const CornerUpLeft = arrowToTopLeft;
export const CornerUpRight = arrowToTopRight;
export const Cpu = cpu;
export const Crop = crop;
export const Crosshair = alias(Solar.Target);
export const Database = database;
export const Delete = alias(Solar.TrashBin2);
export const Disc = record;
export const Divide = minusSquare;
export const DivideCircle = minusCircle;
export const DivideSquare = minusSquare;
export const DollarSign = dollar;
export const DownloadCloud = download;
export const Dribbble = camera;
export const Droplet = dropper;
export const Edit2 = pen;
export const Edit3 = pen;
export const Figma = figma;
export const Facebook = camera;
export const FastForward = arrowRight;
export const Feather = pen;
export const FileArchive = archive;
export const FileAudio = musicNote;
export const FileCode = code;
export const FileImage = gallery;
export const FileMinus = fileRemove;
export const FilePlus = addSquare;
export const FileSpreadsheet = fileText;
export const FileVideo = videoFrame;
export const Film = videoFrame;
export const Filter = filter;
export const Flag = flag;
export const FolderMinus = removeFolder;
export const Framer = figma;
export const Frown = sadCircle;
export const Gift = gift;
export const GitCommit = branchDown;
export const GitMerge = branchUp;
export const GitPullRequest = branchDown;
export const Github = code;
export const Gitlab = code;
export const Grid = threeSquares;
export const GridIcon = threeSquares;
export const Headphones = headphones;
export const Hexagon = windowFrame;
export const Heart = heart;
export const Instagram = camera;
export const Layout = windowFrame;
export const LifeBuoy = questionCircle;
export const Link2 = link;
export const LinkIcon = link;
export const Linkedin = link;
export const LogIn = login;
export const Map = map;
export const MapPin = mapPoint;
export const Meh = sadCircle;
export const Mic = microphone;
export const MicOff = microphone;
export const Minimize = minimize;
export const Sun = sun;
export const Telescope = telescope;
export const Key = key;
export const Quote = chatRound;
export const Redo = arrowRight;
export const Twitter = chatRound;
export const Undo = arrowLeft;
export const UserCog2 = settingsMinimalistic;
export const UsersRound = alias(Solar.UsersGroupRounded);
export const Youtube = videoFrame;
